# chart-releaser configuration (https://github.com/helm/chart-releaser)
# release-notes-file is resolved relative to each chart directory. The file is
# generated by scripts/extract-release-notes.js during the release workflow and
# is not committed; charts without it fall back to their Chart.yaml description.
release-notes-file: RELEASE_NOTES.md
