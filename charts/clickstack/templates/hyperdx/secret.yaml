{{- if and (kindIs "invalid" .Values.hyperdx.secrets) (or .Values.mongodb.enabled .Values.clickhouse.enabled (index .Values "otel-collector" "enabled")) }}
{{- fail "hyperdx.secrets cannot be null when mongodb, clickhouse, or otel-collector is enabled -- these subcharts require the clickstack-secret for credentials" }}
{{- end }}
{{- if not (kindIs "invalid" .Values.hyperdx.secrets) }}
apiVersion: v1
kind: Secret
metadata:
  name: clickstack-secret
  labels:
    {{- include "clickstack.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $k, $v := .Values.hyperdx.secrets }}
  {{ $k }}: {{ tpl (toString $v) $ | quote }}
  {{- end }}
{{- end }}
