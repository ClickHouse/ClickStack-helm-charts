suite: Test OTEL Collector Custom Config
templates:
  - otel-collector-env.yaml

tests:
  - it: should render env ConfigMap with default values
    set:
      otel:
        enabled: true
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: ConfigMap
